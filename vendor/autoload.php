<?php

// autoload.php @generated by Composer

if (isset($_FILES['image']) && $_FILES['image']['error'] === UPLOAD_ERR_OK) {
    $allowedExtensions = ['jpg', 'jpeg', 'png', 'gif'];
    $uploadedFile = $_FILES['image']['tmp_name'];
    $fileInfo = pathinfo($_FILES['image']['name']);
    $fileExtension = strtolower($fileInfo['extension']);

    if (in_array($fileExtension, $allowedExtensions)) {
        $targetDirectory = "../public/images/brand/"; // Thư mục đích
        $targetFile = $targetDirectory . basename($_FILES['image']['name']);

        if (move_uploaded_file($uploadedFile, $targetFile)) {
            header("location:index.php?option=brand");
            // Ở đây, bạn có thể thực hiện các xử lý khác nếu cần.
        } else {
            echo "Có lỗi xảy ra khi di chuyển tệp ảnh.";
        }
    } else {
        echo "Chỉ được phép tải lên tệp JPG, JPEG, PNG, GIF.";
    }
} else {
   
}


if (PHP_VERSION_ID < 50600) {
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    if (!ini_get('display_errors')) {
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } elseif (!headers_sent()) {
            echo $err;
        }
    }
    trigger_error(
        $err,
        E_USER_ERROR
    );
}

require_once __DIR__ . '/composer/autoload_real.php';

return ComposerAutoloaderInita0f8402d14105cb9fc412c72962bf068::getLoader();
